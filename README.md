# diy-graphql-server

This is an example of a simple, yet fully functional GraphQL server.

## Requirements

Node 19

## Setup

`> npm i`

## Run dev (watch) mode

`> npm run dev`

## Run

`> npm start`
